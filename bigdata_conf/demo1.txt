val data = sc.textFile("hdfs://nn.c:9000/tmp/demo2.csv").map(line => line.split(",")).map(userinfo => (userinfo(0), userinfo(1), userinfo(2), userinfo(3)))

val uniqueIp = data.map{ case(mac, ip, cmd, fullcmd) => ip }.distinct().count()
